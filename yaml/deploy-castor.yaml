apiVersion: apps/v1
kind: Deployment
metadata:
  name: castor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: castor
  template:
    metadata:
      labels:
        app: castor
    spec:
      containers:
      - name: castor
        image: pangealab/castor:latest
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: castor
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: castor          
